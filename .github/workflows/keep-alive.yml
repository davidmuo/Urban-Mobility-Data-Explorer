name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s https://urban-mobility-data-explorer-y6h6.onrender.com/api/trips/?per_page=1 > /dev/null
